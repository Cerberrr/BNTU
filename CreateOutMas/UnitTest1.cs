using System;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using WheatstoneCode;

namespace CreateOutMasTest
{
    [TestClass]
    public class Test1
    {
        [TestMethod]
        public void TestMethod1()
        {
            // надо протестировать например процедуру CreateOutMas, подставляя там 
            // в строку keyText = Convert.ToString(textBox1.Text).ToUpper();
            // вместо textBox1.Text какие то значения
            


        }
    }
}
